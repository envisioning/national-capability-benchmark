import type { Config } from '@envisioning/app'
import { navigation } from './navigation'
import { schema } from './schema'
import { settings } from './settings'

export const config: Config = { navigation, schema, settings }
